🎵 Tune Senseسیستم هوشمند تشخیص ژانر موسیقی👥 اعضای پروژهعلی بادپروا (Ali Badparva)(در صورت وجود اعضای دیگر، نام آن‌ها در این بخش اضافه می‌شود)📌 معرفی پروژهTune Sense یک سیستم هوشمند برای تشخیص ژانر موسیقی است که با تحلیل فایل‌های صوتی، ژانر موسیقی را با استفاده از تکنیک‌های یادگیری ماشین و استخراج ویژگی‌های مبتنی بر یادگیری عمیق پیش‌بینی می‌کند.در این پروژه، به‌جای استفاده از ویژگی‌های سطح پایین صوتی، از Embeddingهای مدل از پیش آموزش‌دیده YAMNet برای استخراج ویژگی‌های معنایی صوت استفاده شده و سپس یک مدل Support Vector Machine (SVM) برای دسته‌بندی ژانرها آموزش داده شده است. همچنین یک رابط کاربری گرافیکی (GUI) جهت تعامل ساده و مؤثر با سیستم پیاده‌سازی شده است.🧠 ایده و منطق اصلی پروژهدر پروژه Tune Sense به‌جای استفاده‌ی صرف از ویژگی‌های سنتی صوت (مثل MFCC)، از مدل از پیش آموزش‌دیده YAMNet برای استخراج embeddingهای صوتی استفاده شده است.YAMNet یک مدل یادگیری عمیق مبتنی بر CNN است که روی دیتاست AudioSet گوگل آموزش دیده و توانایی استخراج ویژگی‌های سطح بالا و معنایی از صدا را دارد. این ویژگی‌ها سپس به یک مدل SVM داده می‌شوند تا عمل دسته‌بندی ژانر انجام شود.مزایای این ترکیب:استفاده از قدرت مدل‌های عمیق (Deep Learning).آموزش سریع، سبک و قابل کنترل.🎯 این پروژه دقیقاً چه کاری انجام می‌دهد؟این سیستم به کاربر اجازه می‌دهد تا:🎧 یک فایل صوتی (.wav یا .mp3) انتخاب کند.🧠 ویژگی‌های صوتی معنادار را به‌صورت خودکار استخراج کند.🤖 ژانر موسیقی را با استفاده از مدل آموزش‌دیده پیش‌بینی کند.📊 میزان اطمینان (Confidence) هر ژانر را مشاهده کند.🕓 تاریخچه‌ی فایل‌های تحلیل‌شده را مشاهده کند.🔁 مدل را مستقیماً از داخل رابط کاربری آموزش دهد (بدون نیاز به ترمینال).🛠 روند اجرای سیستمبارگذاری فایل صوتی: فایل برای سازگاری با YAMNet به نرخ نمونه‌برداری 16kHz تبدیل می‌شود.استخراج ویژگی‌ها با YAMNet: استخراج ویژگی‌های سطح بالا و معنایی.نرمال‌سازی ویژگی‌ها: استفاده از StandardScaler برای پایداری عملکرد مدل.دسته‌بندی با SVM: استفاده از یک مدل SVM با کرنل RBF.محاسبه‌ی میزان اطمینان: خروجی شامل احتمال تعلق فایل صوتی به هر ژانر.رابط کاربری گرافیکی: تجربه‌ای کاربرپسند مبتنی بر Tkinter.📂 ساختار پروژهPlaintextmusic-AI/
│
├── data/
│   └── genres_original/       # دیتاست (دسته‌بندی‌شده بر اساس ژانر)
│
├── model/
│   ├── svm_model.pkl          # مدل آموزش‌دیده SVM
│   ├── scaler.pkl             # اسکیلر ویژگی‌ها
│   └── label_encoder.pkl      # انکودر برچسب ژانرها
│
├── predict.py                 # منطق پیش‌بینی و استخراج ویژگی
├── train_model.py             # اسکریپت آموزش مدل
├── ui.py                      # رابط کاربری گرافیکی
└── README.md                  # مستندات پروژه
📂 توضیح فایل‌ها و نقش هر کدام1️⃣ feature_extraction.pyاین فایل مربوط به روش کلاسیک استخراج ویژگی صوتی (آزمایشی) شامل:MFCC, Chroma, Spectral Contrast, Tempo, Mel Spectrogram.2️⃣ predict.pyهسته‌ی منطق پیش‌بینی:بارگذاری Lazy مدل (فقط هنگام نیاز).استخراج ویژگی با YAMNet و نرمال‌سازی.محاسبه Confidence.3️⃣ train_model.pyمسئول آموزش کامل مدل:خواندن دیتاست از ساختار فولدری.آموزش SVM با پارامترهای: kernel=RBF, probability=True, class_weight=balanced.ذخیره مدل، Scaler و Label Encoder.4️⃣ ui.pyرابط کاربری گرافیکی با قابلیت‌های:انتخاب فایل و نمایش نوار پیشرفت.نمایش تاریخچه با اسکرول افقی/عمودی.اجرای آموزش در Thread جداگانه برای جلوگیری از فریز شدن UI.🧪 جزئیات آموزش و نتایجمدل استخراج ویژگی: YAMNet (از TensorFlow Hub)مدل دسته‌بندی: Support Vector Machine (RBF Kernel)دقت روی داده‌ی آموزش: حدود ۹۹٪دقت روی داده‌ی تست: حدود ۸۸ تا ۹۰٪ (تعادل مناسب برای جلوگیری از Overfitting)🧰 کتابخانه‌های استفاده‌شدهکتابخانهدلیل انتخابlibrosaبارگذاری، Resample و پردازش اولیه سیگنال صوتیtensorflowاستفاده از مدل YAMNet و استخراج Embeddingscikit-learnپیاده‌سازی SVM، نرمال‌سازی و تقسیم داده‌هاjoblibذخیره و بارگذاری سریع مدل‌هاtkinterرابط کاربری گرافیکی سبک و بومیthreadingاجرای موازی آموزش مدل و UI⚙️ الزامات و پیش‌نیازهاPython نسخه 3.9 یا بالاتراتصال اینترنت (فقط برای بار اول جهت دانلود YAMNet)حداقل 4GB RAM توصیه می‌شودساختار دیتاست: هر ژانر داخل یک فولدر جداگانه در مسیر data/genres_original/🖥 نحوه اجرای پروژه1️⃣ آموزش مدلاز طریق ترمینال:Bashpython train_model.py
یا از طریق دکمه‌ی Train Model در رابط کاربری.2️⃣ اجرای برنامهBashpython ui.py
3️⃣ پیش‌بینی ژانر موسیقیانتخاب فایل صوتی.انتظار برای پردازش.مشاهده ژانر پیش‌بینی‌شده و میزان اطمینان در تاریخچه.
