🎵 Tune Sense
سیستم هوشمند تشخیص ژانر موسیقی

👥 اعضای پروژه

علی بادپروا (Ali Badparva)

(در صورت وجود اعضای دیگر، نام آن‌ها در این بخش اضافه می‌شود)

📌 معرفی پروژه

Tune Sense یک سیستم هوشمند برای تشخیص ژانر موسیقی است که با تحلیل فایل‌های صوتی، ژانر موسیقی را با استفاده از تکنیک‌های یادگیری ماشین و استخراج ویژگی‌های مبتنی بر یادگیری عمیق پیش‌بینی می‌کند.

در این پروژه، به‌جای استفاده از ویژگی‌های سطح پایین صوتی، از Embeddingهای مدل از پیش آموزش‌دیده YAMNet برای استخراج ویژگی‌های معنایی صوت استفاده شده و سپس یک مدل Support Vector Machine (SVM) برای دسته‌بندی ژانرها آموزش داده شده است.
همچنین یک رابط کاربری گرافیکی (GUI) جهت تعامل ساده و مؤثر با سیستم پیاده‌سازی شده است.

🧠 ایده و منطق اصلی پروژه

در پروژه Tune Sense به‌جای استفاده‌ی صرف از ویژگی‌های سنتی صوت (مثل MFCC)، از مدل از پیش آموزش‌دیده YAMNet برای استخراج embeddingهای صوتی استفاده شده است.

YAMNet یک مدل یادگیری عمیق مبتنی بر CNN است که روی دیتاست AudioSet گوگل آموزش دیده و توانایی استخراج ویژگی‌های سطح بالا و معنایی از صدا را دارد.
این ویژگی‌ها سپس به یک مدل SVM داده می‌شوند تا عمل دسته‌بندی ژانر انجام شود.

این ترکیب باعث شده:

از قدرت مدل‌های عمیق استفاده شود

ولی آموزش سریع، سبک و قابل کنترل باقی بماند

🎯 این پروژه دقیقاً چه کاری انجام می‌دهد؟

این سیستم به کاربر اجازه می‌دهد تا:

🎧 یک فایل صوتی (.wav یا .mp3) انتخاب کند
🧠 ویژگی‌های صوتی معنادار را به‌صورت خودکار استخراج کند
🤖 ژانر موسیقی را با استفاده از مدل آموزش‌دیده پیش‌بینی کند
📊 میزان اطمینان (Confidence) هر ژانر را مشاهده کند
🕓 تاریخچه‌ی فایل‌های تحلیل‌شده را مشاهده کند
🔁 مدل را مستقیماً از داخل رابط کاربری آموزش دهد (بدون نیاز به ترمینال)

🧠 ایده‌ی اصلی و روند اجرای سیستم

بارگذاری فایل صوتی
فایل صوتی انتخاب‌شده بارگذاری شده و برای سازگاری با YAMNet به نرخ نمونه‌برداری 16kHz تبدیل می‌شود.

استخراج ویژگی‌ها با YAMNet
به‌جای استفاده صرف از ویژگی‌هایی مانند MFCC، از مدل عمیق YAMNet برای استخراج ویژگی‌های سطح بالا و معنایی استفاده می‌شود.

نرمال‌سازی ویژگی‌ها
ویژگی‌های استخراج‌شده با استفاده از StandardScaler نرمال‌سازی می‌شوند تا عملکرد مدل پایدارتر شود.

دسته‌بندی با SVM
یک مدل SVM با کرنل RBF برای تشخیص ژانر موسیقی آموزش داده شده است.

محاسبه‌ی میزان اطمینان
خروجی مدل شامل احتمال تعلق فایل صوتی به هر ژانر است که امکان تحلیل شفاف‌تر نتایج را فراهم می‌کند.

رابط کاربری گرافیکی
رابط کاربری مبتنی بر Tkinter تجربه‌ای ساده، سریع و کاربرپسند فراهم می‌کند.

📂 ساختار پروژه
music-AI/
│
├── data/
│   └── genres_original/       # دیتاست (دسته‌بندی‌شده بر اساس ژانر)
│
├── model/
│   ├── svm_model.pkl          # مدل آموزش‌دیده SVM
│   ├── scaler.pkl             # اسکیلر ویژگی‌ها
│   └── label_encoder.pkl      # انکودر برچسب ژانرها
│
├── predict.py                 # منطق پیش‌بینی و استخراج ویژگی
├── train_model.py             # اسکریپت آموزش مدل
├── ui.py                      # رابط کاربری گرافیکی
├── README.md                  # مستندات پروژه

📂 توضیح فایل‌ها و نقش هر کدام
1️⃣ feature_extraction.py

این فایل مربوط به روش کلاسیک استخراج ویژگی صوتی است و شامل موارد زیر می‌شود:

MFCC

Chroma

Spectral Contrast

Tempo

Mel Spectrogram

این بخش بیشتر جنبه‌ی آزمایشی و مقایسه‌ای دارد و نشان می‌دهد پروژه قابلیت توسعه به روش‌های مختلف استخراج ویژگی را دارد.

2️⃣ predict.py

این فایل هسته‌ی منطق پیش‌بینی ژانر است و شامل:

بارگذاری lazy مدل (Load only when needed)

استخراج ویژگی با YAMNet

نرمال‌سازی ویژگی‌ها با scaler ذخیره‌شده

پیش‌بینی ژانر با SVM

محاسبه‌ی confidence برای هر ژانر

نکته مهم:
مدل فقط زمانی load می‌شود که واقعاً نیاز باشد.
اگر مدل train نشده باشد، خطای کنترل‌شده به UI برگردانده می‌شود.

3️⃣ train_model.py

این فایل مسئول آموزش کامل مدل است و مراحل زیر را انجام می‌دهد:

خواندن دیتاست از ساختار فولدری
(هر فولدر = یک ژانر)

استخراج embedding صوتی با YAMNet

Encode کردن لیبل‌های ژانر

نرمال‌سازی ویژگی‌ها

تقسیم داده به train و test

آموزش مدل SVM با:

kernel = RBF

probability = True

class_weight = balanced

ذخیره مدل، scaler و label encoder

این فایل کاملاً مستقل است و هم از طریق ترمینال و هم از طریق UI اجرا می‌شود.

4️⃣ ui.py

این فایل رابط کاربری گرافیکی پروژه را پیاده‌سازی می‌کند.

قابلیت‌ها:

انتخاب فایل موسیقی

اجرای پیش‌بینی ژانر

نمایش ژانر تشخیص‌داده‌شده

نمایش درصد اطمینان هر ژانر

نمایش نوار پیشرفت

نمایش تاریخچه‌ی آهنگ‌های پردازش‌شده

آموزش مدل با دکمه‌ی Train Model

اجرای آموزش در Thread جدا برای جلوگیری از فریز شدن UI

همچنین:

اسکرول افقی و عمودی برای تاریخچه در نظر گرفته شده

خطاهای مهم به‌صورت MessageBox نمایش داده می‌شوند

🧪 جزئیات آموزش مدل

مدل استخراج ویژگی: YAMNet (از TensorFlow Hub)

مدل دسته‌بندی: Support Vector Machine (کرنل RBF)

متعادل‌سازی کلاس‌ها: فعال

ارزیابی: تقسیم داده به Train و Test

بارگذاری هوشمند: مدل فقط در زمان نیاز بارگذاری می‌شود

📊 نتایج نمونه آموزش

دقت روی داده‌ی آموزش: حدود 99٪

دقت روی داده‌ی تست: حدود 88 تا 90٪

این نتایج نشان‌دهنده‌ی تعادل مناسب بین یادگیری و جلوگیری از بیش‌برازش (Overfitting) هستند.

🧰 کتابخانه‌های استفاده‌شده و دلیل انتخاب آن‌ها

librosa
برای بارگذاری فایل‌های صوتی، Resample کردن صدا و پردازش اولیه سیگنال صوتی

tensorflow و tensorflow_hub
برای استفاده از مدل از پیش آموزش‌دیده YAMNet و استخراج embeddingهای سطح بالا از صدا

scikit-learn
برای پیاده‌سازی SVM، نرمال‌سازی ویژگی‌ها، Label Encoding و تقسیم داده‌ها

joblib
برای ذخیره و بارگذاری سریع مدل‌ها و کاهش زمان load نسبت به pickle

tkinter
برای پیاده‌سازی رابط کاربری گرافیکی سبک، سازگاری بالا با سیستم‌عامل‌ها و عدم نیاز به کتابخانه جانبی

threading
برای اجرای آموزش مدل بدون قفل شدن UI

subprocess
برای اجرای train_model.py از داخل رابط گرافیکی

⚙️ الزامات و پیش‌نیازهای پروژه

Python نسخه 3.9 یا بالاتر

اتصال اینترنت (فقط برای بار اول دانلود YAMNet)

دیتاست موسیقی با ساختار:
هر ژانر داخل یک فولدر جدا

حداقل 4GB RAM توصیه می‌شود

🖥️ نحوه اجرای پروژه
1️⃣ آموزش مدل

از طریق ترمینال:

python train_model.py


یا از طریق دکمه‌ی Train Model در رابط کاربری

2️⃣ اجرای برنامه
python ui.py

3️⃣ پیش‌بینی ژانر موسیقی

انتخاب فایل صوتی

انتظار برای پردازش

مشاهده ژانر پیش‌بینی‌شده و میزان اطمینان

بررسی تاریخچه در پنل کناری
