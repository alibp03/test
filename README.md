آها، متوجه شدم! منظور شما این است که وقتی این متن رو در **فایل واقعی README.md** قرار می‌دهید، هِدرها (`##`) و فرمت Markdown درست نمایش داده نمی‌شود یا در ویرایشگر شما به شکل درست ظاهر نمی‌شود. برای اینکه **کاملاً استاندارد و قابل استفاده در GitHub** باشد، باید متن بدون بلاک‌کد اضافی و با **Markdown واقعی** نوشته شود. من نسخه‌ی اصلاح‌شده و قابل استفاده آماده کردم:

---

# 🎵 Tune Sense

**سیستم هوشمند تشخیص ژانر موسیقی**

---

## 👥 اعضای پروژه

* علی بادپروا (Ali Badparva)
  *(در صورت وجود اعضای دیگر، نام آن‌ها اضافه می‌شود)*

---

## 📌 معرفی پروژه

Tune Sense یک سیستم هوشمند برای تشخیص ژانر موسیقی است که با تحلیل فایل‌های صوتی، ژانر موسیقی را با استفاده از تکنیک‌های یادگیری ماشین و استخراج ویژگی‌های مبتنی بر یادگیری عمیق پیش‌بینی می‌کند.

* از **Embeddingهای مدل از پیش آموزش‌دیده YAMNet** برای استخراج ویژگی‌های معنایی صوت استفاده می‌شود.
* سپس یک **مدل Support Vector Machine (SVM)** برای دسته‌بندی ژانرها آموزش داده می‌شود.
* رابط کاربری گرافیکی (**GUI**) جهت تعامل ساده و مؤثر با سیستم پیاده‌سازی شده است.

---

## 🧠 ایده و منطق اصلی پروژه

* به‌جای ویژگی‌های سنتی صوتی (مثل MFCC)، از **YAMNet** برای استخراج embeddingهای سطح بالا استفاده می‌شود.
* YAMNet یک مدل CNN آموزش‌دیده روی **AudioSet گوگل** است که ویژگی‌های معنایی صوت را استخراج می‌کند.
* خروجی YAMNet به مدل **SVM** داده می‌شود تا ژانر موسیقی پیش‌بینی شود.

**مزیت این ترکیب:**

* استفاده از قدرت مدل‌های عمیق
* آموزش سریع، سبک و قابل کنترل

---

## 🎯 قابلیت‌ها

* انتخاب فایل صوتی (`.wav` یا `.mp3`)
* استخراج خودکار ویژگی‌های صوتی معنادار
* پیش‌بینی ژانر موسیقی با مدل آموزش‌دیده
* مشاهده درصد اطمینان (Confidence) هر ژانر
* مشاهده تاریخچه‌ی فایل‌های تحلیل‌شده
* آموزش مدل از داخل GUI بدون نیاز به ترمینال

---

## 📂 ساختار پروژه

```
music-AI/
│
├── data/
│   └── genres_original/       # دیتاست (دسته‌بندی‌شده بر اساس ژانر)
│
├── model/
│   ├── svm_model.pkl          # مدل آموزش‌دیده SVM
│   ├── scaler.pkl             # اسکیلر ویژگی‌ها
│   └── label_encoder.pkl      # انکودر برچسب ژانرها
│
├── feature_extraction.py      # استخراج ویژگی‌های صوتی کلاسیک
├── predict.py                 # منطق پیش‌بینی و استخراج ویژگی
├── train_model.py             # اسکریپت آموزش مدل
├── ui.py                      # رابط کاربری گرافیکی
├── README.md                  # مستندات پروژه
```

---

## 📂 توضیح فایل‌ها

**feature_extraction.py**

* استخراج ویژگی‌های صوتی کلاسیک: MFCC، Chroma، Spectral Contrast، Tempo، Mel Spectrogram
* بیشتر برای مقایسه و توسعه روش‌های استخراج ویژگی

**predict.py**

* هسته‌ی منطق پیش‌بینی ژانر
* بارگذاری lazy مدل (**Load only when needed**)
* استخراج ویژگی با YAMNet
* نرمال‌سازی ویژگی‌ها با scaler ذخیره‌شده
* پیش‌بینی ژانر با SVM
* محاسبه confidence برای هر ژانر
* خطای کنترل‌شده در صورت نبود مدل آموزش‌دیده

**train_model.py**

* خواندن دیتاست (هر فولدر = یک ژانر)
* استخراج embedding صوتی با YAMNet
* Encode لیبل‌های ژانر
* نرمال‌سازی ویژگی‌ها
* تقسیم داده به train و test
* آموزش SVM:

  * kernel = RBF
  * probability = True
  * class_weight = balanced
* ذخیره مدل، scaler و label encoder
* قابل اجرا هم از ترمینال هم GUI

**ui.py**

* رابط کاربری گرافیکی با Tkinter
* انتخاب فایل موسیقی و اجرای پیش‌بینی
* نمایش ژانر و درصد اطمینان
* نمایش تاریخچه آهنگ‌ها و نوار پیشرفت
* اجرای آموزش مدل در Thread جدا برای جلوگیری از فریز UI
* نمایش خطاها به‌صورت MessageBox

---

## 🧪 جزئیات آموزش مدل

* مدل استخراج ویژگی: **YAMNet** (TensorFlow Hub)
* مدل دسته‌بندی: **SVM** (کرنل RBF)
* متعادل‌سازی کلاس‌ها: فعال
* ارزیابی: تقسیم داده به Train و Test
* بارگذاری هوشمند: فقط در زمان نیاز

**نتایج نمونه آموزش:**

* دقت روی داده‌ی آموزش: ~99٪
* دقت روی داده‌ی تست: ~88-90٪

---

## 🧰 کتابخانه‌های استفاده‌شده

| کتابخانه                    | کاربرد                                      |
| --------------------------- | ------------------------------------------- |
| librosa                     | بارگذاری و پردازش اولیه فایل صوتی           |
| tensorflow / tensorflow_hub | استفاده از YAMNet و استخراج embedding       |
| scikit-learn                | SVM، نرمال‌سازی، Label Encoding، تقسیم داده |
| joblib                      | ذخیره و بارگذاری سریع مدل‌ها                |
| tkinter                     | GUI سبک و سازگار با سیستم‌عامل‌ها           |
| threading                   | اجرای آموزش مدل بدون قفل شدن UI             |
| subprocess                  | اجرای train_model.py از داخل GUI            |

---

## ⚙️ پیش‌نیازها

* Python 3.9 یا بالاتر
* اتصال اینترنت (برای بار اول دانلود YAMNet)
* دیتاست موسیقی با فولدر جداگانه برای هر ژانر
* حداقل 4GB RAM توصیه می‌شود

---

## 🖥️ نحوه اجرا

**1️⃣ آموزش مدل**
ترمینال:

```bash
python train_model.py
```

یا از دکمه **Train Model** در GUI

**2️⃣ اجرای برنامه**

```bash
python ui.py
```

**3️⃣ پیش‌بینی ژانر**

* انتخاب فایل صوتی
* انتظار برای پردازش
* مشاهده ژانر و درصد اطمینان
* بررسی تاریخچه آهنگ‌ها در پنل کناری

```

---

اگر بخواهی، می‌توانم همین **README را با تصاویر، Badges و لینک‌های مستقیم به مدل و دیتاست** هم آماده کنم تا شبیه یک پروژه حرفه‌ای GitHub به نظر برسد و صفحه پروژه جذاب‌تر شود.  

می‌خوای اون نسخه هم بسازم؟
```
