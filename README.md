بسیار عالی. متن‌های ارسالی شما را ترکیب و به یک فایل `README.md` حرفه‌ای، ساختاریافته و استاندارد برای گیت‌هاب تبدیل کردم.

---

# 🎵 Tune Sense

### سیستم هوشمند تشخیص ژانر موسیقی مبتنی بر یادگیری عمیق و SVM

**Tune Sense** یک ابزار هوشمند برای تحلیل و تشخیص ژانر موسیقی است. این پروژه با ترکیب قدرت مدل‌های پیش‌آموزش‌دیده یادگیری عمیق (Deep Learning) و دقت دسته‌بندی‌کننده‌های آماری (Machine Learning)، راهکاری بهینه و سریع برای برچسب‌گذاری خودکار فایل‌های صوتی ارائه می‌دهد.

---

## 👥 اعضای پروژه

* **علی بادپروا (Ali Badparva)**
* (سایر اعضا در این بخش اضافه می‌شوند)

---

## 📌 معرفی پروژه

در این پروژه، به‌جای استفاده از ویژگی‌های سطح پایین صوتی (مانند MFCC به‌تنهایی)، از **Embeddingهای مدل YAMNet** استفاده شده است. YAMNet یک شبکه عصبی عمیق (CNN) است که توسط گوگل روی دیتاست عظیم AudioSet آموزش دیده و توانایی استخراج ویژگی‌های معنایی و سطح بالا از صوت را دارد. این ویژگی‌ها سپس به یک مدل **Support Vector Machine (SVM)** با کرنل RBF تزریق می‌شوند تا عملیات دسته‌بندی ژانر با دقت بالا انجام شود.

---

## 🎯 قابلیت‌های کلیدی

* 🎧 **پشتیبانی از فرمت‌ها:** قابلیت پردازش فایل‌های `.wav` و `.mp3`.
* 🧠 **استخراج ویژگی پیشرفته:** بهره‌گیری از انتقال یادگیری (Transfer Learning) با مدل YAMNet.
* 📊 **تحلیل اطمینان:** نمایش میزان اطمینان (Confidence) برای هر پیش‌بینی.
* 🕓 **تاریخچه فعالیت:** ثبت و نمایش لیست فایل‌های تحلیل‌شده.
* 🔁 **آموزش آسان:** امکان بازآموزی مدل مستقیماً از طریق محیط گرافیکی.
* 🖥️ **رابط کاربری مدرن:** طراحی شده با Tkinter به‌صورت مالتی‌ترید (Multi-threaded) برای جلوگیری از فریز شدن برنامه.

---

## 🧠 ایده و روند اجرای سیستم

1. **بارگذاری و پیش‌پردازش:** فایل صوتی بارگذاری شده و به نرخ نمونه‌برداری **16kHz** (استاندارد YAMNet) تبدیل می‌شود.
2. **استخراج Embedding:** مدل YAMNet ویژگی‌های معنایی صوت را استخراج می‌کند.
3. **نرمال‌سازی:** ویژگی‌ها با استفاده از `StandardScaler` استاندارد می‌شوند.
4. **دسته‌بندی:** مدل SVM با کرنل RBF ژانر نهایی را تشخیص می‌دهد.
5. **خروجی:** نمایش ژانر پیش‌بینی شده به همراه درصد احتمال.

---

## 📂 ساختار پروژه

```text
music-AI/
│
├── data/
│   └── genres_original/       # دیتاست (دسته‌بندی‌شده بر اساس ژانر)
│
├── model/
│   ├── svm_model.pkl          # مدل آموزش‌دیده SVM
│   ├── scaler.pkl             # اسکیلر ویژگی‌ها
│   └── label_encoder.pkl      # انکودر برچسب ژانرها
│
├── predict.py                 # هسته منطق پیش‌بینی و استخراج ویژگی
├── train_model.py             # اسکریپت آموزش مدل
├── ui.py                      # رابط کاربری گرافیکی (GUI)
├── feature_extraction.py      # استخراج ویژگی‌های کلاسیک (جهت مقایسه)
└── README.md                  # مستندات پروژه

```

---

## 📂 شرح وظایف فایل‌ها

* **`predict.py`:** مدیریت بارگذاری هوشمند (Lazy Loading) مدل‌ها. استخراج ویژگی و پیش‌بینی را انجام می‌دهد.
* **`train_model.py`:** مسئول خواندن دیتاست، آموزش SVM با تنظیمات `class_weight='balanced'` و ذخیره مدل‌ها.
* **`ui.py`:** مدیریت پنجره‌ها، دکمه‌ها، نوار پیشرفت (Progress Bar) و اجرای فرآیندها در Thread جداگانه.
* **`feature_extraction.py`:** شامل متدهای استخراج ویژگی‌های سنتی مانند MFCC، Chroma و Spectral Contrast جهت اهداف پژوهشی و مقایسه‌ای.

---

## 📊 جزئیات فنی و نتایج

* **مدل استخراج ویژگی:** YAMNet (TensorFlow Hub)
* **مدل دسته‌بندی:** SVM (RBF Kernel)
* **دقت روی داده آموزش:** ~۹۹٪
* **دقت روی داده تست:** ۸۸٪ تا ۹۰٪ (نشان‌دهنده تعادل عالی و عدم Overfitting)

---

## 🧰 ابزارها و کتابخانه‌ها

| کتابخانه | دلیل استفاده |
| --- | --- |
| **Librosa** | بارگذاری، Resample و پردازش اولیه سیگنال صوتی |
| **TensorFlow Hub** | دسترسی به مدل از پیش آموزش‌دیده YAMNet |
| **Scikit-learn** | پیاده‌سازی SVM، نرمال‌سازی و تقسیم داده‌ها |
| **Joblib** | ذخیره و بارگذاری سریع مدل‌ها (بهینه تر از Pickle) |
| **Tkinter** | ساخت رابط کاربری دسکتاپ سبک و بومی |
| **Threading** | جلوگیری از قفل شدن UI هنگام پردازش‌های سنگین |

---

## ⚙️ پیش‌نیازها و نصب

* Python 3.9 یا بالاتر
* اتصال اینترنت (فقط برای اولین اجرای YAMNet)
* حداقل 4GB رم (توصیه‌شده)

**نصب کتابخانه‌های مورد نیاز:**

```bash
pip install tensorflow tensorflow-hub librosa scikit-learn joblib

```

---

## 🖥️ نحوه اجرا

1. **آموزش مدل:**
ابتدا دیتاست خود را در پوشه `data` قرار داده و سپس مدل را آموزش دهید (از طریق ترمینال یا دکمه Train در UI):
```bash
python train_model.py

```


2. **اجرای برنامه:**
```bash
python ui.py

```



---

## 🚀 نقاط قوت پروژه

* بهره‌گیری از **Transfer Learning** برای دقت بالاتر در داده‌های کم.
* معماری **ماژولار** (امکان تعویض راحت مدل دسته‌بندی).
* رابط کاربری کاربرپسند با قابلیت نمایش تاریخچه و درصد اطمینان.

---

**آیا مایل هستید بخش "نحوه مشارکت در پروژه" یا "مجوز (License)" را هم به این فایل اضافه کنم؟**
